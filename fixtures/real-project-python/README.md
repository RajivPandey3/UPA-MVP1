# Real Python Project Proof Fixture

Dependency-free Python fixture for cross-language proof. The proof must execute
`main.py`, hash the source, detect a controlled mutation, restore the source, and
retain timing and byte-size evidence.
