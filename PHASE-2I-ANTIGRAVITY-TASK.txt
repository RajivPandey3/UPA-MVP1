PHASE 2I — TRUST INSPECTION BOUNDARY

ANTIGRAVITY TASK:
Implement ONLY the API boundary for:

    GET /v1/trust/{id}

Purpose:
- Establish the V1.1 REST inspection boundary.
- Do NOT implement new trust-storage or verification logic.
- Do NOT modify V1.0 core behavior.
- Do NOT push.
- Do NOT commit yet.

Requirements:

1. Add an API-layer response contract for trust inspection.
   - Keep the external REST contract language-neutral.
   - Use snake_case JSON names where fields are exposed.
   - Do not expose V1.0 core objects directly from the controller.

2. Add:
       ITrustInspectionAdapter
   with an async method accepting the trust identifier and CancellationToken.

3. Add a temporary/not-yet-implemented adapter implementation.
   - It must clearly indicate that inspection is not implemented yet.
   - Do not invent storage behavior.
   - Do not read or mutate V1.0 state directly.

4. Add:
       GET /v1/trust/{id}

   Controller behavior:
   - Accept the route identifier.
   - Call ITrustInspectionAdapter.
   - Return 200 if an implementation result is available.
   - For the temporary adapter, return HTTP 501 with the established API error shape/code.
   - Preserve cancellation behavior.
   - Do not change existing POST /v1/trust/emit behavior.
   - Do not change existing POST /v1/trust/verify behavior.

5. Register the inspection adapter in Program.cs using the same DI style already used by the API.

6. Add HTTP tests proving at minimum:
   - GET /v1/trust/{id} reaches the inspection boundary and returns 501 while unimplemented.
   - The route identifier is accepted.
   - Existing API tests remain passing.

7. Verify:
   - V1.0 source/tests/dependencies are untouched.
   - No global JSON serializer policy changes.
   - No unrelated refactoring.
   - No authentication/rate-limit implementation yet.
   - No real inspection/storage implementation yet.

8. Run:
   - API tests
   - full Release build

9. At the end, report:
   - exact files changed
   - test result
   - build result
   - confirmation V1.0 files were untouched
   - confirmation no commit was created

IMPORTANT:
This is ONE focused task only. Stop after the verification report.
